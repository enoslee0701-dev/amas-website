# ============================================================
# AMAS 官网 · 环境变量示例
# 本站为纯静态站点，没有构建步骤：这些值不会被自动注入，
# 实际生效位置是 assets/js/supabase-config.js（见 docs/SUPABASE-SETUP.md）。
# 此文件仅作为"需要哪些值、去哪里取"的清单与备忘。
# ============================================================

# Supabase 控制台 → Project Settings → API → Project URL
# 形如 https://xxxxxxxxxxxx.supabase.co
SUPABASE_URL=

# Supabase 控制台 → Project Settings → API → anon / publishable key
# 这是"可公开"的前端密钥，配合 RLS 使用，允许出现在前端代码里
SUPABASE_ANON_KEY=

# ⛔ 绝对禁止：service_role / secret key 属于服务端密钥，
# 拥有绕过 RLS 的全部权限——不得写入本仓库任何文件、
# 不得出现在前端代码、不得提交到 git。
# SUPABASE_SERVICE_ROLE_KEY=（永远不要填在这里）
