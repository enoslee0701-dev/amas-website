# 队列 · AMAS Web
分支: csc/2026-09-14

### T-001 · 在仓库根目录安装 CSC 自主开发规则文件并设置 `scripts/verify.sh` 可执行
状态: DONE
优先级: GREEN
验收标准:
  - `test -f CLAUDE.md && test -f BLOCKED.md && test -x scripts/verify.sh`

### T-002 · 创建或确认工作分支命名为 `csc/<YYYY-MM-DD>`，保留现有用户改动
状态: DONE
优先级: GREEN
验收标准:
  - `git branch --show-current` 匹配 `^csc/[0-9]{4}-[0-9]{2}-[0-9]{2}$`

### T-003 · 运行仓库现有验证脚本并记录首轮结果
状态: TODO
优先级: GREEN
验收标准:
  - `./scripts/verify.sh` 退出码为 0

### T-004 · 盘点首轮验证覆盖范围，补齐一个缺失的本地回归检查
状态: DONE
优先级: GREEN
验收标准:
  - 新增检查可独立运行且退出码为 0，并在提交中列出命令

### T-005 · 修复首个可本地复现的非 RED 验证失败
状态: DONE
优先级: GREEN
验收标准:
  - 对应复现命令先失败后通过，且未删除或跳过测试

### T-006 · 为已修复问题补充最小回归测试
状态: DONE
优先级: GREEN
验收标准:
  - 回归测试稳定通过并能在 `./scripts/verify.sh` 中执行

### T-007 · 审核仓库文档与运行说明，修正一处与当前行为不一致的说明
状态: DONE
优先级: GREEN
验收标准:
  - 文档改动可由一条本地检查命令验证

### T-008 · 完成当前阶段 checkpoint 提交并生成简短完成报告
状态: DONE
优先级: GREEN
验收标准:
  - 存在对应 `csc: T-008` commit，`git show --stat --oneline HEAD` 成功
作废: Luna 时期遗留

### T-009 · 核对申请人资料页读取失败与空结果的显示边界
状态: DONE
优先级: GREEN
验收标准:
  - 静态检查覆盖读失败、空对象和重试入口

### T-010 · 为账号状态词表增加契约一致性回归检查
状态: DONE
优先级: GREEN
验收标准:
  - 页面键集与 SQL 枚举集合完全一致

### T-011 · 审核保存按钮在途禁用与重复提交防护
状态: DONE
优先级: GREEN
验收标准:
  - 本地 stub 检查增量请求数为 1

### T-012 · 检查申请人历史页的读不到与无记录区分
状态: DONE
优先级: GREEN
验收标准:
  - 两种情形分别显示且对应检查通过

### T-013 · 盘点学员侧同族页面可复用的读失败模式
状态: DONE
优先级: YELLOW
验收标准:
  - 报告列出入口、现状和一个有界候选任务

### T-014 · 为页面异常采集补充最小静态保护检查
状态: NEED_ENOS
优先级: GREEN
验收标准:
  - window.onerror 收集存在且不泄漏敏感值

### T-015 · 审核当前缓存版本号变更范围
状态: DONE
优先级: YELLOW
验收标准:
  - 变更文件仅包含预期缓存标记

### T-016 · 运行资料页受影响的最小本地回归并保存报告
状态: DONE
优先级: GREEN
验收标准:
  - 目标脚本退出码为 0 且报告路径存在
作废: Luna 时期遗留

### T-017 · 为页面 API 错误归一化补充静态边界检查
状态: DONE
优先级: GREEN
验收标准:
  - 未知、权限和限流错误均映射到明确用户提示

### T-018 · 审核申请历史页空数据与读取失败状态
状态: DONE
优先级: GREEN
验收标准:
  - 两种状态分别有本地回归用例和重试入口

### T-019 · 为资料页重复提交防护扩展三种触发方式测试
状态: DONE
优先级: GREEN
验收标准:
  - 点击、回车和 requestSubmit 各只发一次请求

### T-020 · 检查缓存戳脚本对未跟踪 HTML 的保护
状态: DONE
优先级: YELLOW
验收标准:
  - 负向夹具确认不会把用户文件自动纳入提交

### T-021 · 运行全站脚本语法与链接一致性检查
状态: DONE
优先级: GREEN
验收标准:
  - 本地检查退出码为 0，无外网请求

### T-022 · 为无配置启动场景补充友好降级提示测试
状态: DONE
优先级: GREEN
验收标准:
  - 使用非秘密占位配置可稳定复现并通过

### T-023 · 盘点发布前浏览器兼容性检查范围
状态: DONE
优先级: YELLOW
验收标准:
  - 形成有界报告，不执行发布或真实数据操作

### T-024 · 运行 verify.sh 并生成阶段 checkpoint 检查报告
状态: DONE
优先级: GREEN
验收标准:
  - verify.sh 退出码为 0，报告记录固定 SHA
作废: Luna 时期遗留

### T-025 · 为资料页网络失败与空响应补充本地回归
状态: DONE
优先级: GREEN
验收标准:
  - 两种状态分别有断言且退出码为 0

### T-026 · 检查缓存戳生成对路径空格和非跟踪文件的处理
状态: DONE
优先级: GREEN
验收标准:
  - 负向夹具确认不修改用户文件

### T-027 · 为历史记录分页边界补充静态检查
状态: DONE
优先级: GREEN
验收标准:
  - 首尾页与空页均有本地断言

### T-028 · 复核浏览器兼容性报告中的未满足依赖
状态: DONE
优先级: YELLOW
验收标准:
  - 只列依赖与 NOT_RUN，不执行发布

### T-029 · 审核无配置启动时的降级页面与提示
状态: DONE
优先级: GREEN
验收标准:
  - 非秘密占位配置可稳定复现

### T-030 · 为表单重复提交的键盘路径补充回归
状态: DONE
优先级: GREEN
验收标准:
  - 回车与 requestSubmit 各只发一次请求

### T-031 · 盘点真实后端联调前置条件
状态: DONE
优先级: YELLOW
验收标准:
  - 形成有界报告，不改安全设置或真实数据

### T-032 · 运行 verify.sh 与链接一致性检查并生成 checkpoint
状态: DONE
优先级: GREEN
验收标准:
  - 脚本退出码均为 0，记录固定 SHA
作废: Luna 时期遗留
